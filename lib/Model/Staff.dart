import 'package:flutter/material.dart';

class StaffModel{
  final int ? id;
  final String ? nameStaff;
  final String ? staffWork;
  final String ? staffNumber;
  final String ? staffEmail;
  final String ? staffAddress;
  final String ? staffImages;

  StaffModel(this.id, this.nameStaff, this.staffWork, this.staffNumber, this.staffEmail, this.staffAddress, this.staffImages);
}