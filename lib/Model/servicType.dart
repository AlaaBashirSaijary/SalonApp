import 'package:flutter/material.dart';

class ServicType{

  final String ? nametype;
  final String ? images;
  final String ? title;
  bool  clik=false;


  ServicType( this.nametype, this.images, this.title,);
}